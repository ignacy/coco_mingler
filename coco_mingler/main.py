import logging

logger = logging.getLogger("COCO Mingler")


def main(argv=None):
    """Run the coco_mingler command.

    Args:
        argv: optional list of arguments to parse.
        sys.argv is used by default

    Returns:
        int: command's return code
    """
    print("Hello from coco mingler")
